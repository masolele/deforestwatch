import numpy as np

def preprocess_planet(roi):
    # For now, return dummy array. Replace with actual preprocessing.
    H, W = 256, 256
    dummy = np.random.rand(H, W, 17).astype(np.float32)
    return dummy
